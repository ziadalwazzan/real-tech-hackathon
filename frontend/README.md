# Frontend (React + Tailwind)

## Setup
```bash
npm install
```

## Run
```bash
npm run dev
```

## Environment
The frontend expects the API at `http://localhost:8000` by default. Copy `.env.example` to `.env` to override `VITE_API_BASE_URL` if needed.
